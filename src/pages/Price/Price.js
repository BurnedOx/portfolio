import React from 'react';
import Header from '../../components/Header/Header';

const Price = props => {
    return (
        <div>
            <Header/>
            Price
        </div>
    );
}

export default Price;